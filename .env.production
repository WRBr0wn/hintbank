# Production worker URL for multiplayer rooms. Public value, safe to commit.
# Vite loads this during `npm run build`; without it the client stays same-origin
# (the dev proxy). See the deploy runbook in docs/ARCHITECTURE.md.
VITE_ROOMS_URL=https://hintbank-rooms.zenvolka.workers.dev
